<Window x:Class="CPUDoc.Windows.OkDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:adonisUi="clr-namespace:AdonisUI;assembly=AdonisUI"
        xmlns:adonisControls="clr-namespace:AdonisUI.Controls;assembly=AdonisUI"
        xmlns:adonisExtensions="clr-namespace:AdonisUI.Extensions;assembly=AdonisUI"
        xmlns:tb="http://www.hardcodet.net/taskbar"
        adonisExtensions:CursorSpotlightExtension.RelativeSpotlightSize="0"
        FontSize="11"                         
        BorderThickness="1"
        UseLayoutRounding="True"
        ResizeMode="NoResize"
        BorderBrush="{DynamicResource WindowBorderColor}"
        xmlns:local="clr-namespace:CPUDoc"
        mc:Ignorable="d"
        Height="Auto" Width="550"
        Title="{Binding Title}">
    <Window.Resources>
        <!-- Ensure OkCommand is defined as a static resource if not available as a static property -->
        <RoutedUICommand x:Key="OkCommand" Text="Ok" />
    </Window.Resources>
    <Window.Template>
        <ControlTemplate TargetType="Window">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ContentPresenter Grid.Row="0" />
                <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center">
                    <Button Content="Ok"
                            IsDefault="True"
                            Command="{StaticResource OkCommand}" 
                            Margin="{adonisUi:Space}"
                            />
                    <Button Content="Cancel"
                            Margin="{adonisUi:Space}"
                            IsCancel="True" />
                </StackPanel>
            </Grid>
        </ControlTemplate>
    </Window.Template>
</Window>

