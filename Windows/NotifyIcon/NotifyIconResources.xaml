<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:tb="http://www.hardcodet.net/taskbar"
    xmlns:local="clr-namespace:CPUDoc"
    >

    <!--  The taskbar context menu - the first row is a dummy to show off simple data binding  -->
    <!--
        The "shared" directive is needed if we reopen the sample window a few times - WPF will otherwise
        reuse the same context menu (which is a resource) again (which will have its DataContext set to the old TaskbarIcon)
    -->
    <ContextMenu
        x:Shared="false"
        x:Key="SysTrayMenu"
        >
        <MenuItem Header="{Binding Version}" Command="{Binding ShowWindowCommand}"/>
        <Separator />
        <MenuItem Header="Toggle SysSetHack" Command="{Binding ToggleSSHCommand}" x:Name="ToggleSSH"/>
        <MenuItem Header="Toggle PowerSaverActive" Command="{Binding TogglePSACommand}" x:Name="TogglePSA"/>
        <MenuItem Header="Toggle NumaZero" Command="{Binding ToggleNumaZeroCommand}" x:Name="ToggleN0"/>
        <Separator />
        <MenuItem Header="Exit" Command="{Binding ExitApplicationCommand}" />
    </ContextMenu>

    <tb:TaskbarIcon
        Visibility="Collapsed"
        x:Key="NotifyIcon"
        IconSource="/Images/CPUDoc.ico"
        ToolTipText="CPUDoc"
        DoubleClickCommand="{Binding ShowWindowCommand}"
        NoLeftClickDelay="True"
        ContextMenu="{StaticResource SysTrayMenu}"
        >
        <tb:TaskbarIcon.DataContext>
            <local:NotifyIconViewModel/>
        </tb:TaskbarIcon.DataContext>
    </tb:TaskbarIcon>

</ResourceDictionary>